const mobileLogo = document.getElementById('mobile-logo');
const animParagraph = document.querySelector('.anim-paragraph');
const animSubParagraph = document.querySelector('.anim-sub-paragraph')
const navBtn = document.getElementById('nav-btn');

mobileLogo.style.opacity = "1";
mobileLogo.style.transform = 'translateY(0)';

animParagraph.style.opacity = "1";
animParagraph.style.transform = 'translateX(0)';

animSubParagraph.style.opacity = "1";
animSubParagraph.style.transform = 'translateY(0)'

navBtn.style.opacity = "1";
navBtn.style.transform = "translateY(0px)";